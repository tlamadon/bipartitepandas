language: python
python:
  - "3.6"
cache: pip
before_install:
  - pip install codecov
install:
  - pip install .
  - pip install pytwoway
script:
  - python -m pytest --cov-report term --cov=app/test/
after_success:
  - codecov